# Documentation

You learned a bit about the Unison Codebase Manager by looking at a piece
of documentation.

In Unison documentation is a first-class citizen; there is a type called
`Doc` and syntax for creating terms of that type.

If you haven't already, create a project and depend on `booster-2026`. Then
head over to the `doc.README` and explore!
